// Generated client for API group storage\n// TODO: Implement or import a common HTTP request wrapper function (e.g., fetchWrapper)\n/*\nasync function fetchWrapper(method, endpoint, params, needAuth) {\n  const SiyuanKernelPrefix = typeof window === 'object' ? '' : 'http://127.0.0.1:6806';\n  const url = SiyuanKernelPrefix + endpoint;\n  const options = { method, headers: {} };\n  if (method === 'POST' && params && Object.keys(params).length > 0) { // Only add body if params exist and are not empty\n    options.headers['Content-Type'] = 'application/json';\n    options.body = JSON.stringify(params);\n  }\n  if (needAuth) {\n    // Example: Retrieve and add auth token\n    // const token = localStorage.getItem('siyuan-auth-token'); \n    // if (token) options.headers['Authorization'] = `Token ${token}`;\n    options.headers['Authorization'] = 'Bearer YOUR_TOKEN_HERE'; // Placeholder\n  }\n  const response = await fetch(url, options);\n  if (!response.ok) {\n    let errorData = 'Failed to parse error response';\n    try { errorData = await response.json(); } catch (e) { try {errorData = await response.text(); } catch (e2) { /* ignore secondary error */ }}\n    console.error('API Error:', response.status, errorData); \n    throw new Error(`API Error ${response.status}: ${JSON.stringify(errorData)}`);\n  }\n  const contentType = response.headers.get('content-type');\n  if (contentType && contentType.includes('application/json')) {\n    return response.json();\n  } \n  return response.text(); // Or handle other content types\n}\n*/\n\n/**\n * 获取所有用户已保存的搜索标准（过滤条件）列表。\n * (Requires authentication)\n * @param {any} payload - ERROR: API def getCriteria Request returns z.object() directly.\n * @returns {Promise<any>} ERROR: API def getCriteria Response returns z.object() directly.\n */\nexport async function getCriteria(payload) {\n  // Example: return fetchWrapper('POST', '/api/storage/getCriteria', payload, true);\n  console.log('Mock call to getCriteria with:', payload);\n  return Promise.resolve({}); // TODO: Ensure mock response matches Promise<any>\n}\n\n/**\n * 获取浏览器 LocalStorage 中的所有键值对。\n * (Requires authentication)\n * @param {any} payload - ERROR: API def getLocalStorage Request returns z.object() directly.\n * @returns {Promise<any>} ERROR: API def getLocalStorage Response returns z.object() directly.\n */\nexport async function getLocalStorage(payload) {\n  // Example: return fetchWrapper('POST', '/api/storage/getLocalStorage', payload, true);\n  console.log('Mock call to getLocalStorage with:', payload);\n  return Promise.resolve({}); // TODO: Ensure mock response matches Promise<any>\n}\n\n/**\n * 获取用户最近打开过的文档列表。这些文档按最近打开时间排序。\n * (Requires authentication)\n * @param {any} payload - ERROR: API def getRecentDocs Request returns z.object() directly.\n * @returns {Promise<any>} ERROR: API def getRecentDocs Response returns z.object() directly.\n */\nexport async function getRecentDocs(payload) {\n  // Example: return fetchWrapper('POST', '/api/storage/getRecentDocs', payload, true);\n  console.log('Mock call to getRecentDocs with:', payload);\n  return Promise.resolve({}); // TODO: Ensure mock response matches Promise<any>\n}\n\n/**\n * 根据名称移除一个已保存的搜索标准（过滤条件）。\n * (Requires authentication, Requires admin role, Unavailable in read-only mode)\n * @param {any} payload - ERROR: API def removeCriterion Request returns z.object() directly.\n * @returns {Promise<any>} ERROR: API def removeCriterion Response returns z.object() directly.\n */\nexport async function removeCriterion(payload) {\n  // Example: return fetchWrapper('POST', '/api/storage/removeCriterion', payload, true);\n  console.log('Mock call to removeCriterion with:', payload);\n  return Promise.resolve({}); // TODO: Ensure mock response matches Promise<any>\n}\n\n/**\n * 根据提供的键名列表，批量移除浏览器 LocalStorage 中的项目。同时会广播事件通知其他客户端同步移除。\n * (Requires authentication, Requires admin role, Unavailable in read-only mode)\n * @param {any} payload - ERROR: API def removeLocalStorageVals Request returns z.object() directly.\n * @returns {Promise<any>} ERROR: API def removeLocalStorageVals Response returns z.object() directly.\n */\nexport async function removeLocalStorageVals(payload) {\n  // Example: return fetchWrapper('POST', '/api/storage/removeLocalStorageVals', payload, true);\n  console.log('Mock call to removeLocalStorageVals with:', payload);\n  return Promise.resolve({}); // TODO: Ensure mock response matches Promise<any>\n}\n\n/**\n * 保存或更新一个搜索标准（过滤条件）。搜索标准可用于后续的文档或内容搜索。\n * (Requires authentication, Requires admin role, Unavailable in read-only mode)\n * @param {any} payload - ERROR: API def setCriterion Request returns z.object() directly.\n * @returns {Promise<any>} ERROR: API def setCriterion Response returns z.object() directly.\n */\nexport async function setCriterion(payload) {\n  // Example: return fetchWrapper('POST', '/api/storage/setCriterion', payload, true);\n  console.log('Mock call to setCriterion with:', payload);\n  return Promise.resolve({}); // TODO: Ensure mock response matches Promise<any>\n}\n\n/**\n * 使用一个完整的对象来覆盖整个浏览器 LocalStorage 的内容。通常用于导入或恢复 LocalStorage 数据。同时会广播事件通知其他客户端同步设置。\n * (Requires authentication, Requires admin role, Unavailable in read-only mode)\n * @param {any} payload - ERROR: API def setLocalStorage Request returns z.object() directly.\n * @returns {Promise<any>} ERROR: API def setLocalStorage Response returns z.object() directly.\n */\nexport async function setLocalStorage(payload) {\n  // Example: return fetchWrapper('POST', '/api/storage/setLocalStorage', payload, true);\n  console.log('Mock call to setLocalStorage with:', payload);\n  return Promise.resolve({}); // TODO: Ensure mock response matches Promise<any>\n}\n\n/**\n * 设置浏览器 LocalStorage 中的单个键值对。同时会广播事件通知其他客户端同步设置。\n * (Requires authentication, Requires admin role, Unavailable in read-only mode)\n * @param {any} payload - ERROR: API def setLocalStorageVal Request returns z.object() directly.\n * @returns {Promise<any>} ERROR: API def setLocalStorageVal Response returns z.object() directly.\n */\nexport async function setLocalStorageVal(payload) {\n  // Example: return fetchWrapper('POST', '/api/storage/setLocalStorageVal', payload, true);\n  console.log('Mock call to setLocalStorageVal with:', payload);\n  return Promise.resolve({}); // TODO: Ensure mock response matches Promise<any>\n}\n\n