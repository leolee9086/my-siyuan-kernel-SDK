// Generated client for API group ref\n// TODO: Implement or import a common HTTP request wrapper function (e.g., fetchWrapper)\n/*\nasync function fetchWrapper(method, endpoint, params, needAuth) {\n  const SiyuanKernelPrefix = typeof window === 'object' ? '' : 'http://127.0.0.1:6806';\n  const url = SiyuanKernelPrefix + endpoint;\n  const options = { method, headers: {} };\n  if (method === 'POST' && params && Object.keys(params).length > 0) { // Only add body if params exist and are not empty\n    options.headers['Content-Type'] = 'application/json';\n    options.body = JSON.stringify(params);\n  }\n  if (needAuth) {\n    // Example: Retrieve and add auth token\n    // const token = localStorage.getItem('siyuan-auth-token'); \n    // if (token) options.headers['Authorization'] = `Token ${token}`;\n    options.headers['Authorization'] = 'Bearer YOUR_TOKEN_HERE'; // Placeholder\n  }\n  const response = await fetch(url, options);\n  if (!response.ok) {\n    let errorData = 'Failed to parse error response';\n    try { errorData = await response.json(); } catch (e) { try {errorData = await response.text(); } catch (e2) { /* ignore secondary error */ }}\n    console.error('API Error:', response.status, errorData); \n    throw new Error(`API Error ${response.status}: ${JSON.stringify(errorData)}`);\n  }\n  const contentType = response.headers.get('content-type');\n  if (contentType && contentType.includes('application/json')) {\n    return response.json();\n  } \n  return response.text(); // Or handle other content types\n}\n*/\n\n/**\n * 获取指定块ID的反向链接和反向提及列表。此接口为旧版，建议使用 /api/ref/getBacklink2。\n * (Requires authentication)\n * @param {any} payload - ERROR: API def getBacklink Request returns z.object() directly.\n * @returns {Promise<any>} ERROR: API def getBacklink Response returns z.object() directly.\n */\nexport async function getBacklink(payload) {\n  // Example: return fetchWrapper('POST', '/api/ref/getBacklink', payload, true);\n  console.log('Mock call to getBacklink with:', payload);\n  return Promise.resolve({}); // TODO: Ensure mock response matches Promise<any>\n}\n\n/**\n * 获取指定块ID的反向链接和反向提及列表，支持排序。\n * (Requires authentication)\n * @param {any} payload - ERROR: API def getBacklink2 Request returns z.object() directly.\n * @returns {Promise<any>} ERROR: API def getBacklink2 Response returns z.object() directly.\n */\nexport async function getBacklink2(payload) {\n  // Example: return fetchWrapper('POST', '/api/ref/getBacklink2', payload, true);\n  console.log('Mock call to getBacklink2 with:', payload);\n  return Promise.resolve({}); // TODO: Ensure mock response matches Promise<any>\n}\n\n/**\n * 获取指定 定义块 在某个特定 文档树 内的反向链接列表。用于在打开一个文档时，显示该文档中有哪些块引用了当前面板的文档。\n * (Requires authentication)\n * @param {any} payload - ERROR: API def getBacklinkDoc Request returns z.object() directly.\n * @returns {Promise<any>} ERROR: API def getBacklinkDoc Response returns z.object() directly.\n */\nexport async function getBacklinkDoc(payload) {\n  // Example: return fetchWrapper('POST', '/api/ref/getBacklinkDoc', payload, true);\n  console.log('Mock call to getBacklinkDoc with:', payload);\n  return Promise.resolve({}); // TODO: Ensure mock response matches Promise<any>\n}\n\n/**\n * 获取指定 定义块 在某个特定 文档树 内的反向提及列表 (提及了定义块的名称或别名，但未直接引用的块)。\n * (Requires authentication)\n * @param {any} payload - ERROR: API def getBackmentionDoc Request returns z.object() directly.\n * @returns {Promise<any>} ERROR: API def getBackmentionDoc Response returns z.object() directly.\n */\nexport async function getBackmentionDoc(payload) {\n  // Example: return fetchWrapper('POST', '/api/ref/getBackmentionDoc', payload, true);\n  console.log('Mock call to getBackmentionDoc with:', payload);\n  return Promise.resolve({}); // TODO: Ensure mock response matches Promise<any>\n}\n\n/**\n * 手动触发指定块的反向链接和提及关系的刷新计算。通常在数据发生变更后，系统会自动更新，但此接口可用于强制刷新。\n * (Requires authentication)\n * @param {any} payload - ERROR: API def refreshBacklink Request returns z.object() directly.\n * @returns {Promise<any>} ERROR: API def refreshBacklink Response returns z.object() directly.\n */\nexport async function refreshBacklink(payload) {\n  // Example: return fetchWrapper('POST', '/api/ref/refreshBacklink', payload, true);\n  console.log('Mock call to refreshBacklink with:', payload);\n  return Promise.resolve({}); // TODO: Ensure mock response matches Promise<any>\n}\n\n